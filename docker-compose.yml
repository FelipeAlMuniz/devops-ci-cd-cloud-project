services:
  app:
    build: .
    ports:
      - "${APP_PORT:-8080}:5000"
    volumes:
      - ./data:/data
    environment:
      - ENVIRONMENT=production
      - TASKS_DB_PATH=/data/tasks.db
    restart: always
